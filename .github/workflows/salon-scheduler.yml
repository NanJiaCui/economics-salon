name: Advance the daily AI salon

on:
  schedule:
    # Three daily stages plus one recovery run after each stage, Asia/Shanghai.
    - cron: "30 0,5,10 * * *"
    - cron: "17 1,6,11 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: daily-ai-salon
  cancel-in-progress: false

jobs:
  advance:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Advance the current salon stage
        env:
          SALON_URL: https://economics-salon-yfcui.jijicyf.chatgpt.site
        run: node scripts/advance-public-salon.mjs
