# Optional: configure these as server-side secrets in Sites to enable live generation.
# With no key, the complete product runs in explicitly labelled teaching-demo mode.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
