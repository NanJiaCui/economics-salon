import {database,identity,json,day,input,apiError,owned} from '@/lib/server';
import {topic,candidates,thinkers} from '@/lib/content';
import {env} from 'cloudflare:workers';
export async function POST(req:Request){try{const user=await identity();const b=await input(req);const db=database();const now=Date.now();if(b.action==='new'){const id=crypto.randomUUID();await db.prepare('INSERT INTO sessions (id,owner,title,mode,round,status,created,updated) VALUES (?,?,?,?,0,?,?,?)').bind(id,user.userId,topic,(env as any).OPENAI_API_KEY?'live':'demo','ready',now,now).run();return json({id});}if(b.action==='vote'){if(!candidates.some(t=>t.id===b.topic))throw new Error('议题不存在。');const h=Number(new Intl.DateTimeFormat('en-GB',{timeZone:'Asia/Shanghai',hour:'2-digit',hourCycle:'h23'}).format(new Date()));const m=Number(new Intl.DateTimeFormat('en-GB',{timeZone:'Asia/Shanghai',minute:'2-digit'}).format(new Date()));if(h===23&&m>=55)throw new Error('今日投票已于 23:55 截止。');const r=await db.prepare('INSERT INTO votes (id,owner,day,topic) SELECT ?,?,?,? WHERE (SELECT COUNT(*) FROM votes WHERE owner=? AND day=?)<5').bind(crypto.randomUUID(),user.userId,day(),b.topic,user.userId,day()).run();if(!r.meta.changes)throw new Error('今天的 5 票已用完，明天再来。');return json({ok:true});}const s=await owned(String(b.session||''),user.userId);if(b.action==='question'){const body=String(b.body||'').trim();if(body.length<5||body.length>500)throw new Error('问题请控制在 5–500 字。');if(!['host',...thinkers.map(t=>t.id)].includes(b.target))throw new Error('请选择有效的提问对象。');if(s.status==='complete')throw new Error('本场已结束，请开启新一场再提问。');const id=crypto.randomUUID();const inserted=await db.prepare('INSERT INTO questions (id,session,owner,body,target,status,created) SELECT ?,?,?,?,?,?,? WHERE (SELECT COUNT(*) FROM questions WHERE session=?)<30').bind(id,s.id,user.userId,body,b.target,'queued',now,s.id).run();if(!inserted.meta.changes)throw new Error('本场问题已达 30 条上限。');return json({id});}if(b.action==='like'){const q=await db.prepare('SELECT id FROM questions WHERE id=? AND session=?').bind(b.question,s.id).first();if(!q)throw new Error('问题不存在。');await db.prepare('INSERT OR IGNORE INTO likes (id,question,owner) VALUES (?,?,?)').bind(crypto.randomUUID(),q.id,user.userId).run();return json({ok:true});}throw new Error('不支持的操作。');}catch(e){return apiError(e)}}
